# Quick deployment checklist

1. `npm install`
2. Create `.env` and add `VITE_TMDB_API_TOKEN=...`
3. `npm run dev` to test.
4. Create a public GitHub repository.
5. Push this folder.
6. Import the repository into Vercel.
7. Add `VITE_TMDB_API_TOKEN` under Environment Variables.
8. Deploy.
